# Bug Fix Log

## 2026-09-05：注册后未自动登录、压力测试商品详情不存在

### 问题现象

- 新用户完成注册后仍停留在未登录状态，需要再次进入登录页手动登录。
- 商品页面点击压力测试商品后，详情页显示 `COMMON_NOT_FOUND`；用户反馈后台无报错，怀疑前端存在静态商品数据。

### 根因

- 注册接口按既有契约只返回用户标识，前端注册分支没有后续登录请求，因此不会写入 Redis 登录令牌。
- 前端商品卡片使用服务端返回的商品 ID，并没有静态商品详情；当前数据库只有压力测试夹具，初始化脚本没有正式商品和 SKU 数据，旧页面或旧 ID 会得到 `COMMON_NOT_FOUND`。

### 修复方案

- 注册成功后调用现有登录接口并保存返回的 Token，再跳转首页。
- 在数据库初始化脚本中加入幂等的演示分类、商品、SKU 和库存数据，保留压力测试夹具并避免固定 ID 冲突。

### 验证结果

- `frontend`: `npm test -- --run` 通过，11 个测试通过；`npm run build` 通过。
- 实际网关链路验证：注册临时用户后立即登录，再调用 `/api/users/me` 成功返回该用户；验证用户已清理。
- 实际商品链路验证：当前数据库补充为 9 个可售商品、9 个 SKU 和对应库存；列表中每个商品 ID 请求详情均成功，压力测试商品详情也成功。
- 后端 Maven 测试未执行：当前环境未安装 Maven，仓库也未提供 Maven Wrapper（`mvn` 不可用）。

## 2026-09-05：商品/分类乱码、图片加载失败、结算地址缺少入口、余额支付

### 问题现象

- 商品和分类中文在前端显示为乱码，商品主图无法正常加载。
- 确认订单页面不能直接新增收货地址，导致没有地址时无法提交订单。
- 当前支付链路依赖模拟支付；希望改为用户默认拥有 10000 余额，下单后从余额扣款。

### 根因

- 商品种子数据通过 PowerShell 管道灌入 MySQL 时发生了字符集转换，数据库接口最终返回 `æ...` 形式的错误字符串；服务 JDBC URL 也未显式声明 UTF-8。
- 商品图片依赖第三方外链，存在防盗链或网络不可达；页面没有失败回退。
- 结算页只加载地址，没有提供新增地址入口。
- 原支付成功链路没有用户余额账户和服务端扣款步骤。

### 修复方案

- 商品/用户等 JDBC 连接显式使用 UTF-8；重新以原始 UTF-8 SQL 文件灌入当前数据库；前端按响应 charset 解码并提供图片失败占位。
- 确认订单页增加新增收货地址表单，保存后自动刷新并选中地址。
- 在用户表增加 `balance` 余额字段，新注册用户和历史用户默认余额为 10000.00。
- 保留现有两阶段模拟支付接口，在模拟成功/成功回调阶段由用户服务原子扣款；支付服务以 `payNo` 做扣款幂等保护，余额不足时不推进支付、订单和库存状态。
- 补充用户余额扣款内部接口、支付/订单回归契约测试，并验证后端构建与测试。

### 验证结果

- 前端 `npm run lint`、`npm test -- --run`（14 项）和 `npm run build` 均通过。
- 三个新增后端余额/支付契约测试均通过；使用 IntelliJ 内置 Maven 执行全量测试时，既有商品模块测试在 Java 21 + 旧 Byte Buddy 兼容性处失败，新增测试本身通过。
- 使用 IntelliJ 内置 Maven 执行 `mvn -q -DskipTests package` 编译通过。
- `git diff --check` 通过，MySQL 8.0.46 兼容的余额字段迁移已验证；当前数据库商品中文已恢复正确存储。
- 当前已运行的 IDEA 服务仍是旧进程，需重启 product/user/pay 服务后加载新代码和 JDBC 配置；重启后再做真实余额支付链路验收。

## 2026-09-05：浏览器 MCP 复核商品乱码

### 验证结果

- 通过 Playwright 浏览器访问 `http://localhost:8088/products`，DOM 中商品名和分类已经是 `æ...` 乱码。
- 浏览器抓取的 `/api/products`、`/api/categories` 响应正文同样包含乱码，说明不是浏览器字体或 React 渲染问题。
- 商品图片请求出现 `net::ERR_BLOCKED_BY_ORB`，确认第三方图片外链还存在独立的跨源阻断问题。
- MySQL 中对应种子数据已是正确中文；当前 8088 网关仍转发到未重启的旧 product Java 进程。需要重启 product 服务加载新的 UTF-8 JDBC 配置后复测。

## 2026-09-05：重启后种子商品仍乱码、压力测试商品详情不存在

### 问题现象

- 重启后，种子商品和分类仍显示乱码，压力测试商品名称正常。
- 点击压力测试商品后详情返回商品不存在。

### 根因

- 种子数据导入时 MySQL 客户端没有声明 `utf8mb4`，中文被写成了双重编码字节；压力测试商品是旧的正确数据，所以表现不同。
- 压力测试商品 ID `5643018474422449472` 超过 JavaScript 安全整数范围，后端返回 JSON 数字后被浏览器解析并截断为 `5643018474422449000`，详情请求因此找不到商品。

### 修复方案

- 为 SQL 初始化脚本增加 `SET NAMES utf8mb4`，并用 `--default-character-set=utf8mb4` 修复当前数据库种子数据。
- 商品主键及 SKU/参数中的商品引用改为 JSON 字符串序列化，前端类型支持字符串 ID，避免 JavaScript 大整数精度丢失。

### 验证结果

- MySQL HEX、product 服务 8082、网关 8088 和浏览器 MCP 对比确认：修复导入后商品与分类接口返回正常中文。
- 正确压力测试 ID 请求 `/api/products/5643018474422449472` 返回 200；错误截断 ID 返回 `COMMON_NOT_FOUND`，复现并确认精度丢失根因。
- 前端 lint、14 项测试、生产构建通过；后端 `mvn -DskipTests package` 编译通过。
- 需要重启 product 服务加载最新的 JSON 字符串 ID序列化代码，再用浏览器刷新确认压力测试商品详情链接。

## 2026-09-05：购物车历史商品名乱码

### 问题现象

- 商品列表和详情中文已恢复正常，但购物车页面仍有部分商品名称乱码。

### 根因

- 购物车 Redis 保存了加入购物车时的 `productName` 快照，`GET /api/cart` 原先直接返回旧快照，不会重新从商品服务同步名称；历史乱码因此一直残留。

### 修复方案

- `GET /api/cart` 查询时按 SKU 重新读取商品名称、商品 ID 和价格，并回写 Redis；失效商品保留在购物车，交由结算校验报告。

### 验证结果

- 待补充。

## 2026-09-05：下单后购物车商品未移除

### 问题现象

- 商品购买完成后，已购买商品仍然显示在购物车中。

### 根因

- 待定位订单创建、库存确认和购物车清理调用链。

### 修复方案

- 订单创建成功后，订单服务按本次订单提交的 SKU 调用购物车服务删除对应项；未选中的商品不会被删除。
- 新增订单购物车清理契约测试。

### 验证结果

- `mvn -DskipTests package` 编译通过。
- `CartCleanupContractTest` 通过；服务重启后需进行真实下单回归。
